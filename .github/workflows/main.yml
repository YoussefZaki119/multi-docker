name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t youssefzaki/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true youssefzaki/react-test npm test

      - run: docker build -t youssefzaki/multi-client ./client
      - run: docker build -t youssefzaki/multi-nginx ./nginx
      - run: docker build -t youssefzaki/multi-server ./server
      - run: docker build -t youssefzaki/multi-worker ./worker

      - run: docker push youssefzaki/multi-client
      - run: docker push youssefzaki/multi-nginx
      - run: docker push youssefzaki/multi-server
      - run: docker push youssefzaki/multi-worker
